doctrine:
  dbal:
    default_connection: mysql
    connections:
      mysql:
        driver:   mysql
        host:     localhost
        port:     3306
        dbname:   database
        username: user
        password: pass
      sqlite:
        driver:   sqlite
        memory:   true
        dbname:   database
        username: user
        password: pass
